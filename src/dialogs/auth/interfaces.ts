interface IRopcResponse {
    id_token: string;
    access_token: string;
    refresh_token: string;
    expires_in: number;
}

interface IRopcResponseObj {
    idToken: string;
    accessToken: string;
    refreshToken: string;
    expiresIn: number;
}
